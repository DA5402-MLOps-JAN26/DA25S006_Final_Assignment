global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers: []

rule_files: []

scrape_configs:
  - job_name: "resume_api"
    static_configs:
      - targets: ["localhost:9001"]
    metrics_path: "/metrics"